//Test that the component renders properly and matches snapshot

//Test to check that searchString state updates when the user is typing

//Test that pressing the enter key calls the mock API function + with the current searchString